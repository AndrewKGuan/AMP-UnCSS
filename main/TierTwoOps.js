/**
 * @file Optimizer that handles advanced CSS optimizations requiring a DOM parser including:
 * - Making !important styles have maximum specificity
 * - Converting inline style attributes into CSS classes
 * Only instantiated and used if UnCss config is 2 or higher
 * @version 0.1
 */

class TierTwoOps{

  /**
   * @param {Object} config - An object containing configuration setting
   */
  constructor({}){

  }



}

module.exports = TierTwoOps;