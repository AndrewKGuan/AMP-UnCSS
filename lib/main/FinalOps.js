/**
 * @file Optimizer that does final file parsing including compression and minification.
 * @version 0.1
 */

class FinalOps{

  /**
   * @param {Object} config - An object containing configuration setting
   * @param {boolean} config.minify - If true, include this.minify() in call list
   * @param {boolean} config.compress - If true, include this.compress() in call list
   */
  constructor({}){

  }

  /**
   * Runs all files through a minification step.
   */
  minify() {

  }
  /**
   * Runs all files through a compression step.
   */
  compress() {

  }


}

module.exports = FinalOps;